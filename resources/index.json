{
    "functions": {
        "uploadImage": "${self:custom.stackPrefix}-images-upload",
        "removeImage": "${self:custom.stackPrefix}-images-remove",
        "uploadFile": "${self:custom.stackPrefix}-files-upload",
        "getFile": "${self:custom.stackPrefix}-files-get",
        "removeFile": "${self:custom.stackPrefix}-files-remove"
    },
    "tables": {
        "metadata": "${self:custom.stackPrefix}-metadata"
    },
    "s3": {
        "fileStorage": "${self:custom.stackPrefix}-files-storage"
    }
}